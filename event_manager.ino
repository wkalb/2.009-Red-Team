// model state of arms/body
// state: open vs closed

// struct for events that we register
// priority
// run function
// inputs that need to match (list of true/false functions? boolean that would be faster to check?)
// length of input list

// process queue of a given length, removes it from the list
// could be linked list or could be a wrap-around buffer

// register events, if event is triggered, run it's function

void setup() 
{
	// create event queue
	// register all events of interest
	
}

void loop() 
{
	// run through entire queue
}

// interrupt
// check for events